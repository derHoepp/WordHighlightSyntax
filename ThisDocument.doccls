Option Explicit

Private WithEvents App As Word.Application

Sub once()
    Set App = Application
End Sub

Private Sub App_WindowSelectionChange(ByVal Sel As Selection)
    Debug.Print Sel.Range.Start
End Sub
